set previewer lf-ueberzug-previewer
set cleaner lf-ueberzug-cleaner

# Open image, video or pdf
cmd open ${{
    case $(file --mime-type -Lb $f) in
        image/*) imv $fx ;;
        video/*) mpv $f ;;
        application/pdf) zathura $f ;;
        *) xdg-open $f ;;
    esac
}}
